const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,root=document.documentElement;document.getElementById('year').textContent=new Date().getFullYear();addEventListener('pointermove',e=>{root.style.setProperty('--x',e.clientX+'px');root.style.setProperty('--y',e.clientY+'px')},{passive:true});const header=document.querySelector('body>header'),knock=document.querySelector('.knock');knock.onclick=()=>{const open=header.classList.toggle('open');knock.setAttribute('aria-expanded',open)};document.querySelectorAll('nav a').forEach(a=>a.onclick=()=>{header.classList.remove('open');knock.setAttribute('aria-expanded','false')});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('seen');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.querySelector('.wiseguy').onclick=()=>{document.body.classList.remove('wise-mode');void document.body.offsetWidth;document.body.classList.add('wise-mode');setTimeout(()=>document.body.classList.remove('wise-mode'),1500)};
const jobs={inbox:['Inbox triage contract','Consigliere classifies incoming messages','Operator drafts and routes responses','Enforcer tracks anything still unanswered'],research:['Research brief contract','Consigliere investigates the question','Operator organizes sources and findings','Enforcer delivers the brief on schedule'],followup:['Follow-up patrol contract','Consigliere identifies open loops','Operator prepares the next actions','Enforcer keeps watch until each one closes'],content:['Content operation contract','Consigliere researches the audience','Operator assembles the first draft','Enforcer runs the publishing checklist']};document.querySelectorAll('[data-job]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-job]').forEach(x=>x.classList.toggle('active',x===b));const j=jobs[b.dataset.job];document.getElementById('job-title').textContent=j[0];document.getElementById('job-list').innerHTML=j.slice(1).map(x=>`<li>${x}</li>`).join('')});
if(!reduced){const bot=document.querySelector('[data-depth]');bot.addEventListener('pointermove',e=>{const r=bot.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5;bot.querySelector('img').style.transform=`translateX(${x*10}px) rotateY(${x*4}deg)`});bot.onpointerleave=()=>bot.querySelector('img').style.transform='';const c=document.getElementById('smoke'),ctx=c.getContext('2d');let w,h,p=[];function size(){w=innerWidth;h=innerHeight;c.width=w;c.height=h;p=Array.from({length:18},()=>({x:Math.random()*w,y:Math.random()*h,r:40+Math.random()*100,v:.05+Math.random()*.15,a:.008+Math.random()*.018}))}function draw(){ctx.clearRect(0,0,w,h);for(const q of p){q.x+=q.v;if(q.x-q.r>w)q.x=-q.r;const g=ctx.createRadialGradient(q.x,q.y,0,q.x,q.y,q.r);g.addColorStop(0,`rgba(232,219,192,${q.a})`);g.addColorStop(1,'transparent');ctx.fillStyle=g;ctx.beginPath();ctx.arc(q.x,q.y,q.r,0,7);ctx.fill()}requestAnimationFrame(draw)}addEventListener('resize',size);size();draw()}
